
export enum VDir
{
    NONE,
    UP,
    DOWN
}

export enum ByTo
{
    BY,
    TO
}

export enum MovingType
{
    LINEAR,
    BEZIER
}

export enum StartUpMode
{
    NONE,
    ON_LOAD,
    START,
    ENABLE, 
    ONLOAD_ENABLE,
    START_ENABLE 
}

export namespace graphic
{
    export enum DrawMode
    {
        FILL_ONLY,
        STROKE_ONLY,
        BOTH
    }
}
